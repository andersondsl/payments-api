# Development environment.
development:
  MONGO_URI: 'mongodb://localhost:27017/payments_database'
  # Port to use
  PORT: 3000
  LOG_LEVEL: debug

# Running tests
test:
  # Disable logging for tests
  LOG_LEVEL: off

# Production environment.
production:
  # Port to use
  PORT: 80
